ion-view(view-title="Accounts")
  ion-content
    ion-list
      ion-item.item-thumbnail-left(ng-repeat="user in users" ng-click="select(user)")
        img(ng-src="{{::user.picture}}")
        h2 {{::user.name}}
        

