# Grab-a-Beer

## Requirements

* node v4.2.2
* `npm install -g npm cordova gulp sass ionic`
* clone the project
* `cd grababeer`
* `npm install`

## Development

* `ionic serve [--address 0.0.0.0 #from vagrant]`
